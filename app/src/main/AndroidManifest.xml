<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.user.firebasedemo">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="com.htd.mapdemo.permission.MAPS_RECIEVE" />
    <uses-permission android:name="com.google.android.providers.gsf.permissions.READ_GSERVICES" />

    <permission
        android:name="com.htd.mapdemo.permission.MAPS_RECIEVE"
        android:protectionLevel="signature" />

    <uses-feature
        android:glEsVersion="0x00020000"
        android:required="true" />
    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality. 
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="DisasterAid"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".firstPage">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="com.theartofdev.edmodo.cropper.CropImageActivity"
            android:theme="@style/Base.Theme.AppCompat" />
        <activity android:name=".Profile" />
        <activity android:name=".Login" />
        <activity android:name=".register_activity" />
        <activity android:name=".Profile_info" />
        <activity android:name=".Profile_Info2" />
        <activity
            android:name=".Homepage"
            android:theme="@android:style/Theme.Holo.Light" />
        <activity android:name=".Icon_act" />
        <activity android:name=".MainActivity" />
        <activity android:name=".UpImage" />
        <activity android:name=".PostActivity" />
        <activity android:name=".blog_app" />
        <activity android:name=".DonateBlog" />
        <activity android:name=".Input" />
        <activity android:name=".BlogSingleActivity" />
        <activity android:name=".ProfilePage" />
        <activity android:name=".SetupActivity" />
        <activity android:name=".Check" />
        <activity android:name=".SignUp" />
        <activity android:name=".MemberDetails" />
        <activity android:name=".EditProfile" />
        <activity android:name=".FbgmLogin" />
        <activity android:name=".MyPost" />
        <activity android:name=".DonateHome" />
        <activity android:name=".DonateSingleActivity" />
        <activity android:name=".DonateInfo" />
        <activity android:name=".CollectDonate" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/. 
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name=".Mapdemo"
            android:label="@string/title_activity_mapdemo" />
        <activity
            android:name=".MapsActivity"
            android:label="@string/title_activity_maps" />
        <activity android:name=".CollectSingle"></activity>
    </application>

</manifest>